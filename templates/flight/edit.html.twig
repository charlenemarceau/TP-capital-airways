{% extends 'base.html.twig' %}

{% block title %}Mofifier un vol{% endblock %}

{% block body %}
    <div class="container bg-secondary">
    <h1 class="text-center mt-3">Modifier le vol : <br>{{ flight.flightNumber }}</h1>
        <div class="row justify-content-center">
            <div class="col-8 justify-content-center mt-5">
        {% for message in app.flashes ('success')%}
        <div class="alert alert-success my-3">
            {{ message }}
        </div>
            {% endfor %}
             {{ include('flight/_form.html.twig', {'button_label': 'Update'}) }}
            {{ include('flight/_delete_form.html.twig') }}
            <div class="text-center"><a href="{{ path('flight_index') }}" class="btn btn-warning" >Retour à la liste</a></div>

            </div>
        </div>
    </div>

{% endblock %}
