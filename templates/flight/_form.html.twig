{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-danger">{{ button_label|default('Save') }}</button>
    <a href="{{ path('flight_index') }}" class="btn btn-success">Retour à la liste</a>
{{ form_end(form) }}
